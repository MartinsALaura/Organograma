import { TextField } from './textField';
/*Esse modelo de exportação, com dois arquivos: textfield.js (contendo o(s) componente(s)) e
index.js (contendo o componente a ser exportado) se torna melhor quando há vários componentes,
 pois aí se torna mais fácil escolher qual componente vai ser exportado para o app.js.
Fica mais organizado. 
*/
export default TextField//exportar function/componente textField